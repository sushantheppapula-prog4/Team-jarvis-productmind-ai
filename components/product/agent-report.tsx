"use client";

import { useState, useTransition } from "react";
import { ArrowRight } from "lucide-react";
import { askProductAgent, type AgentMessage, type AgentResponse } from "@/app/(routes)/dashboard/product/agent-actions";

const prompts = ["WHY IS MY PRODUCT FAILING?", "WHAT SHOULD WE IMPROVE?", "WHAT SHOULD WE BUILD NEXT?", "WHEN SHOULD WE LAUNCH?", "WHERE SHOULD WE EXPAND?", "WHAT CHANGED?"];
export function AgentReportView({ productId, productName, initialResponse }: { productId: string; productName: string; initialResponse: AgentResponse | null }) { const [messages, setMessages] = useState<AgentMessage[]>([]); const [question, setQuestion] = useState(""); const [response, setResponse] = useState<AgentResponse | null>(initialResponse); const [error, setError] = useState(""); const [isPending, startTransition] = useTransition(); const ask = (value = question) => { const q = value.trim(); if (!q) return; setQuestion(""); setError(""); startTransition(async () => { const result = await askProductAgent(productId, q, messages); if (result.error || !result.response) { setError(result.error || "UNABLE TO ANSWER"); return; } setResponse(result.response); setMessages((items) => [...items, { role: "user", content: q }, { role: "assistant", content: result.response!.answer }]); }); }; return <main className="mx-auto max-w-5xl space-y-8 bg-[#F9F9F7] p-8 lg:p-12"><header className="border-b-4 border-[#111111] pb-6"><p className="font-mono text-[10px] uppercase tracking-[0.3em] text-[#CC0000]">AI AGENT / {productName}</p><h1 className="mt-3 font-serif text-5xl font-black uppercase text-[#111111]">AI Agent</h1><p className="mt-4 max-w-3xl text-lg leading-7">Ask Clyra about the selected product’s saved intelligence.</p></header><section className="border-2 border-[#111111] p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-[#525252]">Quick prompts</p><div className="mt-4 flex flex-wrap gap-2">{prompts.map((prompt) => <button key={prompt} onClick={() => ask(prompt)} disabled={isPending} className="border border-[#111111] px-3 py-2 font-mono text-[10px] uppercase tracking-widest hover:bg-[#111111] hover:text-[#F9F9F7] disabled:opacity-50">{prompt}</button>)}</div></section><section className="space-y-5">{messages.map((message, index) => <div key={`${message.role}-${index}`} className={`border-2 border-[#111111] p-5 ${message.role === "assistant" ? "bg-[#111111] text-[#F9F9F7]" : "bg-[#F9F9F7]"}`}><p className="font-mono text-[10px] uppercase tracking-widest text-[#CC0000]">{message.role === "assistant" ? "CLYRA RESPONSE" : "USER MESSAGE"}</p><p className="mt-3 whitespace-pre-wrap text-sm leading-7">{message.content}</p></div>)}</section><div className="border-2 border-[#111111] p-4"><div className="flex gap-3"><input value={question} onChange={(event) => setQuestion(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") ask(); }} disabled={isPending} className="flex-1 bg-transparent p-3 font-mono text-sm outline-none" placeholder="Ask about the selected product intelligence..." /><button onClick={() => ask()} disabled={isPending || !question.trim()} className="border-2 border-[#111111] bg-[#111111] px-5 font-mono text-xs font-bold uppercase tracking-widest text-[#F9F9F7] hover:bg-[#CC0000] disabled:opacity-50">{isPending ? "THINKING..." : "ASK CLYRA"}<ArrowRight className="ml-2 inline h-4 w-4" /></button></div></div>{error && <div className="border-2 border-[#CC0000] p-4 font-mono text-xs uppercase tracking-widest text-[#CC0000]">{error}</div>}{response && <section className="space-y-8 border-t-2 border-[#111111] pt-6"><div className="border-2 border-[#111111] bg-[#111111] p-7 text-[#F9F9F7]"><p className="font-mono text-[10px] uppercase tracking-widest text-[#CC0000]">ANSWER</p><p className="mt-4 whitespace-pre-wrap text-lg leading-8">{response.answer}</p></div><div className="grid gap-5 md:grid-cols-2">{response.metrics.map((metric) => <div key={metric.label} className="border-2 border-[#111111] p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-[#525252]">{metric.label}</p><p className="mt-3 font-serif text-3xl">{metric.value}</p></div>)}</div><div className="grid gap-8 md:grid-cols-2"><div><p className="font-mono text-xs font-bold uppercase tracking-widest text-[#CC0000]">KEY EVIDENCE</p><div className="mt-3 space-y-2">{response.evidence.map((item) => <p key={item} className="border-b border-[#D0D0CB] pb-2 text-sm">{item}</p>)}</div></div><div><p className="font-mono text-xs font-bold uppercase tracking-widest text-[#CC0000]">IMPACT</p><p className="mt-3 text-sm leading-7">{response.impact}</p><p className="mt-6 font-mono text-xs font-bold uppercase tracking-widest text-[#CC0000]">RECOMMENDED ACTION</p><p className="mt-3 text-sm leading-7">{response.recommended_action}</p></div></div><p className="font-mono text-[10px] uppercase tracking-widest text-[#525252]">SOURCE: {response.sources.join(" · ") || "EVIDENCE UNAVAILABLE"}</p></section>}</main>; }
